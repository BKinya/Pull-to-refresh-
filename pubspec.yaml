name: pull_to_refresh_trial
description: A new Flutter application.

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ">=2.7.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^0.1.3
  pull_to_refresh: ^1.6.1

dev_dependencies:
  flutter_test:
    sdk: flutter
flutter:
  uses-material-design: true































